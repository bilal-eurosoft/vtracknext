export type IgnitionReport = {
  TimeZone: string;
  VehicleReg: string;
  clientId: string;
  fromDateTime: string;
  period: string;
  reportType: string;
  toDateTime: string;
  unit: string;
};

export type replayreport = {
  TimeZone: string;
  VehicleReg: string;
  cDateTime: string;
  period:lientId: string;
  fromDateTime: string;
  toDateTime: string;
  unit: string;
  date: any;
};
